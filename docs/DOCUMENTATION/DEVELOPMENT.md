User Input → Validate → Calculate → Stress Test → Score → Store → Display
↑ ↓ ↓ ↓ ↓ ↓ ↓
└─── UI ───┘ Core Engine ───┘ └─── Persistence ──┘

## 🚫 FORBIDDEN PATTERNS

- Never put business logic in UI components
- Never hard-code numbers in calculation functions
- Never bypass auto-reject gates
- Never mutate input data in calculations
